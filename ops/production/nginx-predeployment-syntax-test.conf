# Test harness only. The included file remains a sites-available candidate.
pid /tmp/wbd-nginx-test.pid;
error_log stderr;

events {}

http {
    access_log off;
    include /tmp/wbd-workspace-predeployment.conf;
}

